class Player {
    constructor(name, position, points, image, team, opponent) {
      this.name = name;
      this.position = position;
      this.points = points;
      this.image = image;
      this.team = team;
      this.opponent = opponent;
    }
  }

  export default Player;
